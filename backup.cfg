[buildout]
parts +=
    backup

[backup]
recipe = collective.recipe.backup
location = ${buildout:events}/var/backups/filestorage
blob-storage = /data/blobstorage
blobbackuplocation = ${buildout:events}/var/backups/blobstorage
